# CE 项目提示词模板

## 工作模式
- 本项目默认使用 Compound Engineering（CE）作为 AI 工作流。
- CE 是任务编排层，不覆盖用户指令、项目既有规则、runbook 或其他权威文档。
- 同一任务默认只采用一套主工作流；纯信息任务和明确的小任务不强制创建 CE 文档。

## CE 默认工作流
- 核心循环为 `brainstorm -> plan -> work -> simplify -> code-review -> compound`。
- 需求不清、范围未定、方案分叉或未知项较多时使用 `$ce-brainstorm`。
- 需求明确且需要多步实施时使用 `$ce-plan`；已有计划时使用 `$ce-work` 执行。
- Bug 优先使用 `$ce-debug` 复现并定位根因，不机械经过 brainstorm。
- 有实质代码改动时按需使用 `$ce-simplify-code`；重要或高风险改动使用 `$ce-code-review`。
- 只有出现关键决策、复发坑点、有效排查路径或可复用模式时才使用 `$ce-compound`。
- `$lfg`、worktree、功能分支、PR、自动合并和跨模型执行均不是默认行为，只有用户或项目规则明确要求时才使用。

## 产物约定
- 需求澄清与方案收敛：`docs/brainstorms/`
- 统一计划与执行进度：`docs/plans/`
- 重要改动的复核记录：`docs/reviews/`
- 问题沉淀与经验复用：`docs/solutions/`
- CE 运行期中间产物：`.context/compound-engineering/`

## 执行规则
- 规则优先级：当前用户指令 > 项目 `AGENTS.md`、相关 runbook 与项目规范 > CE skill 默认行为 > 全局默认行为。
- 有现成的 brainstorm 或 plan 时优先复用和续写，不重复创建平行文档。
- 中型及以上任务开始改动前先确认对应 plan；明确的小任务允许直接实施并完成聚焦验证。
- 涉及运行、发布、迁移、排障或联调时，先查阅项目已有 runbook；变更其中记录的操作时同步更新。
- 改动完成后至少执行与范围匹配的验证，并检查明显回归或范围漂移。
- 所有文档中的路径引用使用仓库相对路径。

## Context7 使用准则
- 需要官方库或框架资料时，优先使用 Context7，减少依赖不确定来源的信息。
- 先解析准确的库 ID，再拉取文档；遇到歧义时说明筛选理由。
- 只拉取满足当前问题的最小上下文；Context7 不足时再考虑其他手段。

## Chrome DevTools MCP 使用准则
- 需要排查浏览器端行为、排版或网络问题时，优先使用 `chrome-devtools` MCP。
- 调试前明确目标页面与采集目标；获取结果后整理关键观察并引用输出。
- 若 MCP 不支持所需操作或报错，记录已尝试的命令与错误信息，再改用其他手段。

## Subagent 默认策略
- 仅在平台支持、任务可独立拆分且并行确有收益时使用 subagent，不把它作为小任务的默认步骤。
- 主线程负责明确写入边界、整合结果、检查 diff 和执行最终验证。
- 子代理不得回滚他人改动；遇到范围冲突时停止写入并汇报。

## Git 协作简则
- 项目已有 Git 规则时严格遵循项目规则；以下约定仅作为缺省值。
- 开始改文件前检查工作区，保留并隔离与当前任务无关的已有改动。
- 形成可独立解释和回滚的小闭环后及时提交并推送，只暂存当前任务相关文件。
- 提交前运行与改动范围匹配的检查；提交信息默认使用简体中文和 Conventional Commits 前缀。
- 不默认创建 worktree、功能分支或 PR，不强推，也不绕过 Git hooks。
